<?xml version="1.0" encoding="utf-8"?>
<!--
   Backup policy for Android 12+ (API 31 and above).

   The app persists exactly one thing: the Spoolman server address the user typed, in the
   `settings` Preferences DataStore (AppContainer.settingsDataStore → files/datastore/). Carrying
   it to a new phone saves retyping a LAN address and leaks nothing — it is user-entered
   configuration, not a credential, since Spoolman has no login of its own.

   Everything else is derived or transient (the material catalog is a bundled asset; there is no
   spool cache and no write history), so an allowlist of that single file is the whole policy.
-->
<data-extraction-rules>
    <cloud-backup>
        <include domain="file" path="datastore/settings.preferences_pb" />
    </cloud-backup>
    <device-transfer>
        <include domain="file" path="datastore/settings.preferences_pb" />
    </device-transfer>
</data-extraction-rules>
